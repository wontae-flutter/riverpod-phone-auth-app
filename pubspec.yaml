name: firebase_phone_auth_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^4.1.3
  firebase_core: ^2.3.0
  freezed_annotation: ^2.2.0
  flutter_riverpod: ^2.1.1
  pinput: ^2.2.16
  flutter_libphonenumber: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.2.1
  build_runner: ^2.3.2


flutter:
  uses-material-design: true
  assets:
    - assets/